// Utility function for setting and getting values from the database
// ensuring that the values and types are properly accounted for

const db = {
    set: (key: string, value: any) => {
        // TO-DO: sets the corresponding key to value
    },
    get: (key: string) => {
        // TO-Do: returns the value at key, or handles if key 
        // does not exist
    }
}

export default db